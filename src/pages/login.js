import React, {useState, useEffect} from "react";
import { Auth } from "../components/auth";
const Login = () => {
    return(
        <div>
                <Auth/>
        </div>

    )
}

export default Login;